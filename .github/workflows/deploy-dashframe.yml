name: Deploy DashFrame to GitHub Pages

on:
  push:
    branches: [main, master, claude/jira-confluence-dashboard-mYKhN]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: hdpspark/dashframe/package-lock.json

      - name: Install dependencies
        run: npm ci
        working-directory: hdpspark/dashframe

      - name: Build
        run: npx vite build
        working-directory: hdpspark/dashframe

      - name: Copy iPhone preview to dist
        run: cp hdpspark/dashframe/iphone-preview.html hdpspark/dashframe/dist/

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: hdpspark/dashframe/dist

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
